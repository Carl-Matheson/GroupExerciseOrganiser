<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mad.groupexerciseorganiser">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".ui.MainActivity"
            android:label="Teams"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SignupActivity"
            android:label="@string/register"
            android:theme="@style/DarkAppTheme"/>
        <activity
            android:name=".ui.GroupActivity"
            android:label="Group"
            android:theme="@style/DarkAppTheme"/>
        <activity
            android:name=".ui.CreateGroupActivity"
            android:label="Create Group"
            android:theme="@style/DarkAppTheme"/>
        <activity
            android:name=".ui.CreateScheduleActivity"
            android:label="Create Schedule"
            android:theme="@style/DarkAppTheme"/>
        <activity
            android:name=".ui.SearchPlayersActivity"
            android:label="Search for players"
            android:theme="@style/DarkAppTheme"/>
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>
</manifest>